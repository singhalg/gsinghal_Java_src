package arch;

import lab4.Vector;

/**
 * An abstraction for providing force.  Forces are
 * provided by Springs between Masses and by Gravity acting
 * on a given Mass.  The interface lets us treat both objects
 * uniformly in terms of their exerting force on an object.
 *
 */
public interface ForceProvider {
	
	public Vector getForce();

}
